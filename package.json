{
  "name": "@openclaw/learning-loop-reporter",
  "version": "0.5.0",
  "description": "Feishu channel adapter for self-learning-loop daily report markdown",
  "type": "module",
  "main": "dist/cli.js",
  "bin": {
    "learning-loop-reporter": "bin/reporter.sh"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "verify": "npm test && npm run build"
  },
  "files": [
    "dist",
    "bin",
    "fixtures",
    "SKILL.md"
  ],
  "keywords": [
    "openclaw",
    "learning-loop",
    "reporter",
    "feishu"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^3.1.0"
  },
  "dependencies": {
    "gray-matter": "^4.0.3"
  }
}
